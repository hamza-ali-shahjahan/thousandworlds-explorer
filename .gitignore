node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Env / secrets — never commit real keys (.env.example IS committed)
.env
.env.*
!.env.example

# Private admin seeds — real collaborator emails stay out of this public repo
*.local.sql
supabase/admins.local.sql

# build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# personal / local editor + tooling config
.claude/settings.local.json
# dev-server launch config — machine-specific absolute paths, stays local
.claude/launch.json

# autonomous goal-loop process log (internal build journal, not part of the app)
.goal/

# Ed's shared reference code/plots (derived from his internal `xce` package) — kept
# locally to guide the emulator demo build; NOT for the public repo without his OK.
Ed Shared/

# Raw API pulls can be regenerated with `npm run data`
data/raw/
.vercel

# NEVER commit conversations, transcripts, or strategy/discussion notes — this is
# an app repo, not a notebook. Defensive net so such files can't be staged by accident.
NOTES
NOTES.*
*.notes.md
notes/
CONVERSATION*
*-transcript.*
*.transcript.*
transcript*
discussion*
DISCUSSION*
*.chat.md
scratch/
*.scratch.*
